<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Perpustakaan"
        tools:targetApi="31">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC2qHxmOCALO9xvAj_1JcuPaoNZlwCwHwI" />


        <activity
            android:name=".DataActivity"
            android:exported="true">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:exported="true">
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".CekbukuActivity" />
        <activity android:name=".InputActivity" />
        <activity android:name=".TampilActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".RecyclerActivity" />
        <activity android:name=".MatematikaActivity" />
        <activity android:name=".IpaActivity" />
        <activity android:name=".CekbukuIpaa" />
        <activity android:name=".CekbukuIpab" />
        <activity android:name=".CekbukuIpac" />
        <activity android:name=".IpsActivity" />
        <activity android:name=".PknActivity" />
    </application>


</manifest>